# 检查点 1 · 视觉与交互验收报告

> 阶段：视觉 / 交互 / 数据验收（不新增功能、不进入检查点 2，预览服务保持运行）
> 预览地址：**http://127.0.0.1:4173/**（后台运行中）
> 代码状态：`tsc --noEmit` 零错误 · `npm test` 21/21 通过 · `npm run build` 成功

---

## 一、作业 6 种数据状态 ↔ 页面显示状态对应关系

数据层（`src/data/types.ts` 的 `SubmissionStatus`）定义 **6 种数据状态**，页面（`src/lib/format.ts` 的 `SUBMISSION_LABEL` / `SUBMISSION_TONE`）用 **标签 + 色调** 呈现。

### 6 种数据状态 × 展示标签与色调

| 数据状态 (key) | 页面标签 | 展示色调 | 含义 |
|---|---|---|---|
| `pending` | 待提交 | 中性(灰) | 已到课但未交作业 |
| `submitted` | 已提交 | 橙(accent) | 已交，等待教师查看 |
| `to_review` | **待批改** | 浅(weak) | 教师尚未评定 |
| `need_revise` | 需修改 | 红(danger) | 被打回重做 |
| `completed` | **已完成** | 绿(success) | 评定通过 |
| `excellent` | **优秀作品** | 绿(success) | 评定通过且优秀（带"优秀"徽标，区别于普通已完成） |

### 与"4 种显示状态（生命周期阶段）"的对应关系

页面把 6 种数据状态归入 **4 个生命周期阶段** 来阅读（不丢信息，仅聚合展示）：

1. **待提交** ← `pending`
2. **待批改** ← `submitted`（已提交，过渡态）+ `to_review`（**待批改**，核心待办）
3. **需修改** ← `need_revise`
4. **已完成** ← `completed`（**已完成**）+ `excellent`（**优秀作品**，作为"已完成"的高亮子类/徽标）

> 说明：当前作业页（`WorksPage`）为不丢失粒度，**直接渲染全部 6 个独立彩色标签**用于列表与筛选；上述"4 阶段"是阅读层面的聚合口径。三种被特别强调的状态——**待批改、已完成、优秀作品**——在 6 标签与 4 阶段中均完整保留，无一遗漏。

---

## 二、学员列表归属页面与路由 + 返回保留筛选

- **归属页面**：「学员档案（列表）」页，组件 `StudentsListPage`，路由 **`/t/students`**（仅教师端导航可见）。
- **筛选条件**：班级（`class`）+ 学习类别（`cat`，即 正常/需关注/进步明显/基础较强）。
- **返回保留修复**：筛选条件现在**写入 URL 查询参数**（`?class=cl1&cat=behind`，`replace` 写回）。从列表点开任意学员进入档案页（`/t/students/:id`），档案页提供「返回学员列表」按钮（`navigate(-1)` 浏览器返回），回到列表时 URL 中的筛选条件原样保留，无需重选。
  - 此前缺陷：类别筛选仅存组件本地 `useState`，进入档案再返回会清空——已修复。

---

## 三、160 条出勤记录 与 153 条学习记录 的差异 + 界面区分

### 数据构成（已用一次性校验脚本核对后删除）

- **出勤记录 160** = 20 名学员 × 8 个已上完课节（共 24 场次，仅前 8 节 `done`）。
- **学习记录 148** = 剔除以下两类后的课堂学习记录：
  - **缺席 7 条**：出勤状态 `absent` → 不产生课堂学习记录（"缺席无记录"）。
  - **已到课但记录待补 5 条**：学员已出勤、作业状态为 `pending`（未交），尚未生成学习记录（"已到课但记录待补"）。
  - 160 − 148 = 12 = 7（缺席）+ 5（待补）✓

### 界面如何区分（档案「学习记录」Tab + 教师「学习时间线」）

两处均以**考勤为基准合并全部已上场次**，按三种情况渲染：

| 情况 | 界面文案 | 标签色调 |
|---|---|---|
| 缺席 | 「缺席（无学习记录）」+ 说明"该场次学员缺席，不产生课堂学习记录" | 红(danger)「缺席无记录」 |
| 已出勤但无学习记录 | 「已到课 · 记录待补」+ 说明"学员已出勤，但本场学习记录尚未填写（教师可在时间线补全）" | 橙(accent)「已到课但记录待补」 |
| 正常 | 展示完整学习记录（预习/完成度/问题/建议/教师观察） | 正常记录 |

`teacher-profile-records.png` 截图即展示该合并时间线，含「缺席无记录」与「已到课但记录待补」条目。

---

## 四、检查点 2 相关内容：仅显示摘要 + 标记未开放

在学员档案页，CP2 范畴的完整管理功能**只展示摘要并明确标注尚未开放**：

- **能力六维**（能力 Tab）：展示当前六维水平与历史 Sparkline，附摘要说明——
  > "以上为能力摘要；完整能力评估、横向对比与批量管理将在检查点 2 开放。"
- **教师评审**：仅展示 `status === 'confirmed'`（已确认）的评审摘要，附说明——
  > "教师评审完整管理将在检查点 2 开放（当前仅展示已确认摘要）。"

CP2 未开放范围（按既定规划）：能力评估完整页、教师评审管理页、教学沟通、待办预警。本阶段不进入。

---

## 五、关于"不以截图文件大小作为验收依据"

本报告与交付**不以 PNG 文件体积判断视觉质量**。本次截图均为**原尺寸整页**捕获（桌面 1440px 宽 / 手机 390px 宽，`deviceScaleFactor=1`，`fullPage` 不裁切、不拼图、不缩小），仅用于你本地逐页肉眼验收交互与视觉。页面是否达标，请以你在预览地址的实际操作为准。

---

## 六、原尺寸截图清单（screenshots/）

| 文件 | 页面 | 视口 |
|---|---|---|
| `teacher-overview.png` | 教学总览（教师） | 1440 |
| `teacher-profile.png` | 教师视角学员档案·概览 | 1440 |
| `teacher-profile-records.png` | 教师视角学员档案·学习记录（含缺席/待补区分） | 1440 |
| `teacher-students.png` | 学员列表（筛选持久化演示） | 1440 |
| `student-home.png` | 学员学习首页 | 1440 |
| `teacher-works.png` | 作业与作品（教师端） | 1440 |
| `student-works.png` | 作业与作品（学员端） | 1440 |
| `teacher-coursemap.png` | 课程地图（教师端） | 1440 |
| `student-coursemap.png` | 课程地图（学员端） | 1440 |
| `student-home-mobile.png` | 学员学习首页（手机端 390） | 390 |
| `teacher-overview-mobile.png` | 教学总览（手机端 390） | 390 |
| `student-home-behind.png` | 学员学习首页（需关注学员·困难高亮） | 1440 |

> 另含 `teacher-practiceroom.png`（AI 实操教室下钻，演示版 AI 建议标注）。

---

## 七、待你确认事项

1. 上述 5 点澄清是否准确无误（尤其"6 状态 ↔ 4 阶段"的聚合口径是否即你想要）。
2. 原尺寸截图在本地预览的视觉/交互是否符合预期。
3. 确认后我再决定是否进入检查点 2；**当前未进入**。
