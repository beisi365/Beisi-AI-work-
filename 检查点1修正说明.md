# 检查点 1 · 两项小修正说明

> 修正后保持预览运行（http://127.0.0.1:4173/），未新增功能，未进入检查点 2。
> 验证：`tsc --noEmit` 零错误 · `npm test` 21/21 · `npm run build` 成功。

---

## 修正一：pending 仅表"作业未提交"，与出勤解耦

**改动前**：`pending` 仅出现在"已出勤但未交"的学员身上；缺席学员直接不建提交记录，导致 `pending` 在语义上被绑到了"已到课"。

**改动后**：
- 出勤状态**仅由 `attendance` 表判定**（present/late/leave/absent），与作业提交完全无关。
- 每个"已上完课节 + 学员"都会生成一条作业提交记录；**缺席学员同样记为 `pending`**（作业未提交），出勤但未交也记为 `pending`。
- 数据核对：出勤 160、提交 160（含缺席→pending），不再有"缺勤即无提交"的耦合。
- 学习记录时间线（档案"学习记录"Tab + 教师时间线）以考勤为基准合并，三类清晰区分：
  - **缺席无记录**（red）：该场次缺席，无学习记录；
  - **已到课但记录待补**（orange）：已出勤、但本场**学习记录**尚未填写（与作业 `pending` 是两回事）；
  - 正常记录：完整学习记录。

---

## 修正二：合并 submitted 与 to_review

**业务差异澄清**：`submitted`（已提交）与 `to_review`（待批改）在当前流程中**无独立入队/排队的转换环节**——学员提交后直接处于"待教师评定"状态，二者语义重叠，属于无意义重复状态。

**改动**：合并为单一状态 **`to_review`（待批改 = 已提交、待教师评定）**，删除 `submitted`。
- 类型 `SubmissionStatus` 现为 5 态：`pending / to_review / need_revise / completed / excellent`。
- 状态标签与色调、`WorksPage` 状态选项、档案状态切换、总览"待批改"统计同步清理 `submitted`。

---

## 修正三：提交率 / 完成率口径

| 指标 | 口径 | 说明 |
|---|---|---|
| **提交率** | 所有**非 pending** 状态占比（`submissionRate`） | 交了即计，含待批改/需修改/已完成/优秀 |
| **完成率** | 仅 **completed + excellent** 占比（`completionRate`） | 仅计评定通过（含优秀） |

- 两率分母一致：均为"已上完课节绑定的作业"（`doneAssignmentIds`）。
- 新增**完成率**展示位：教学总览（平均完成率）、学员学习首页（我的完成率）、学员档案（完成率进度条）、班级与课程（班级完成率）。
- 数据核对：提交率 ≈ 0.75、完成率 ≈ 0.35（演示数据）。

---

## 状态总览（合并后）

| 数据状态 | 标签 | 色调 | 归属阶段 |
|---|---|---|---|
| `pending` | 待提交 | 中性灰 | 待提交 |
| `to_review` | 待批改 | 浅 | 待批改（含原"已提交"） |
| `need_revise` | 需修改 | 红 | 需修改 |
| `completed` | 已完成 | 绿 | 已完成 |
| `excellent` | 优秀作品 | 绿 | 已完成（高亮） |

> 注：原验收报告中"6 种数据状态"的相关表述，以本说明的 5 态为准；4 个生命周期阶段（待提交 / 待批改 / 需修改 / 已完成含优秀）不变。"待批改""已完成""优秀作品"均保留，无遗漏。
