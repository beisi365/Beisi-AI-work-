# ============================================================
# AI 培训中控 · 多用户地基环境变量
# 复制为 .env（或 .env.local），填入你的 Supabase 项目凭据。
# 未配置时应用自动回退到「纯前端单机 / 演示」模式（LocalDataLayer），行为零变化。
# ============================================================

# Supabase 项目 URL（Supabase 后台 → Project Settings → API → Project URL）
VITE_SUPABASE_URL=

# Supabase anon public key（Project Settings → API → anon public）
# ⚠️ 这是公开前端密钥，安全边界完全依赖 supabase/schema.sql 的 RLS 策略，
#    务必确认 RLS 已对所有业务表开启（schema.sql 末尾已 alter table ... enable row level security）。
VITE_SUPABASE_ANON_KEY=

# ============================================================
# 接入步骤（一次性）
# ============================================================
# 1) 建项目：在 supabase.com 新建一个免费项目，拿到上面的 URL + anon key 填入本文件。
# 2) 跑迁移：打开 Supabase 后台 → SQL Editor，把 supabase/schema.sql 全文粘贴执行一次。
#    —— 该脚本建 21 张表 + profiles 鉴权映射 + RLS 行级隔离 + 注册触发器 + claim_identity 认领函数。
# 3) （可选）灌演示数据：在本机执行
#      SUPABASE_URL=xxx SUPABASE_SERVICE_ROLE_KEY=xxx node scripts/seed-to-supabase.mjs
#    service_role key 拥有完全权限、可绕过 RLS，仅本地迁移用，切勿提交/暴露给前端。
#    灌完后 students / teachers 的 user_id 留空，等待真实用户登录后认领。
# 4) 重启 dev：npm run dev（改了 .env 需重启 vite 才能读取）。
# 5) 登录页会自动出现「邮箱登录 / 注册」面板（isSupabaseEnabled 为真时）。
#
# —— 邮箱确认：Supabase 默认开启「Email confirmations」，注册后需点确认邮件才能登录。
#    想开发/演示免确认：Supabase 后台 → Authentication → Providers → Email → 关掉
#    「Confirm email」（仅测试环境建议，生产建议开启）。
#
# —— 认领演示数据：注册时「认领账号」填 sXX（学员）/ tX（教师）即可绑定已灌入的
#    演示记录（仅限 user_id 仍为空、尚未被认领的记录，先到先得）。留空则创建你自己的全新账号。
# ============================================================
